query GetReferrerRewards($pageCount: Int!, $cursor: String!, $address: String!) {
    entities:
        referrerRewards(
            first: $pageCount
            before: $cursor
            filter: {
                referrer: { equalTo: $address }
            }
        ) {
            nodes {
                referral
                amount
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
}