query GetFiatData(
    $pageCount: Int!,
    $cursor: String!
) {
    entities:
        assets(
            first: $pageCount
            after: $cursor
        ) {
            nodes {
                id
                priceUSD
            }
            pageInfo {
                hasNextPage
                endCursor
            }
        }
}