query GetSbApyInfo($pageCount: Int!, $cursor: String!) {
    entities:
        poolXYKs(
            first: $pageCount
            before: $cursor
        ) {
            nodes {
                id
                strategicBonusApy
            }
            pageInfo {
                endCursor
            }
        }
}